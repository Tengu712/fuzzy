[package]
name = "fuzzy"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1.11.1"
serde = { version = "1.0.219", features = ["derive"] }

[dev-dependencies]
fake = "4.2.0"
insta = { version = "1.42.2", features = ["yaml"] }
